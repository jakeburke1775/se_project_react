function ItemCard(props) {
  console.log(props.item);

  return (
    <div>
      {/* <h2>{item.name}</h2>
      <img src={item.link} alt={item.name} /> */}
    </div>
  );
}

export default ItemCard;
